apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnd.fullname" . }}
  labels:
    {{- include "dnd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnd.selectorLabels" . | nindent 4 }}
